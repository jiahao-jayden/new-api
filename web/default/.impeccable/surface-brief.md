# Unity dark console migration

## Confirmed direction

The user's actual LayerLab GUI Pro - Simple Casual(+PSD) 1.0.7 artwork remains the pinned visual world. The user rejected the prior full-surface rainbow implementation. Correct its hierarchy, not its brand: the source demo separates quiet information supports from bright product faces and meaningful actions. Do not repeat full lavender identity walls, mint forms, cyan parent panels, or bright contrasting nested cards. No all-gray, all-blue, brown/terracotta or route-wide hue replacement. Preserve original source geometry, bevels, colorful items and pictograms.

## Direction contract

THESIS: Restore the pinned Unity demo's information hierarchy; color communicates an action, state or product identity, not a container's existence.

OWN-WORLD: Original navy information frames and inset controls, Rubik/CJK type, cyan primary actions, green success/payment identity, compact purple product/reward accents. No nested saturated surfaces.

STORY: Read identity and balances, locate the setting or product, act through an unmistakable control.

FIRST VIEWPORT: Profile uses a compact identity and metrics header above settings; at 1200px and wider they share one horizontal strip, while narrower views give metrics their own row. Models retain bounded native-colored identity faces with quiet price/detail seats. Wallet groups balance, amount choices and branded payment actions without competing colored walls.

FORM: Corrective rebuild within the user-pinned Unity world, not an identity exploration. Existing routes and inline selection behavior remain.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## First viewport contract

A readable source-backed top status bar; key/model racks with inline inspectors; original colorful icons on a quiet Dock with selected-state emphasis only. Real values, labels and translations survive. Original sprite corners and textures remain sharp. Alipay/WeChat retain blue/green actions. Product identity can own a bounded bright face; prices and detailed information use quiet seats. Parent and child surfaces never compete for attention. Use explicit light ink on dark supports and dark ink on bright controls; audited destructive actions keep white labels.

Profile numeric values and units stay together as metric items wrap. The language selector stacks below its full explanation when its container is 480px or narrower. Currency emphasis is light gold, not a yellow panel. Key inventory cards put the 26px name in an 88px minimum blue banner, with a subordinate 32px original key icon; token/status and quota sit in the quiet lower region. The user rejected a large key-art hero, so names must lead. Details and recent-use rows remain quiet. Model details use a small vendor chip on their navy information support. Wallet keeps original coin/gift items, light-gold monetary facts, green earned rewards, blue/green payment actions and a purple referral-copy action; its narrow order is balance → recharge → referral → plans.

## Scope

All existing current-console routes, administration, transient surfaces and shared UI primitives. Preserve existing route permission rules, monetary values, channel discounts and default API-key grouping. Do not add features, database changes, payments or cloud writes. On the user's latest instruction, the original marketing/landing website is excluded and is restored unchanged.

## Interaction contract

Original prefab ColorTint timing (100ms) is retained on buttons. Native Base UI owns focus, keyboard, form state and accessibility. Sprite toggles get an interruptible 180ms thumb movement; this is a web adaptation, not a claimed packaged Unity animation. Overlay transitions retain reduced-motion support. Semantic icons replace redundant labels only where existing accessible names/tooltips make the action clear; critical facts and ambiguous actions keep words.

The key reveal field and copy tile share a 44px height with centered content and an 8px gap. Destructive buttons use the original red-hue texture with an image-only RGB gain of 0.68 and a white label. Dialogs and alert dialogs combine the Settings Dark source window body with its separate arched cap, transparent centered header and original footer strip; the key editor keeps the original gold-key emblem. Sheets use the source window body without the projecting cap. Keyboard, focus and form behavior remain unchanged.

Create-key composition is now governed by the user's selected third ImageGen result (`../../../.artifacts/key-create-option3/reference.png`): a blue name row above vertically ordered settings, with a left label rail and a shared right control rail. Quota and its unlimited toggle stay together. At narrow widths labels stack above fields; footer actions remain equal and fixed outside the scrolling form. Original Unity sprites and existing behavior take precedence over the mock's invented lighting or icon details.

## Artwork and paint contract

Original PNG bytes, hashes, dimensions, alpha and nine-slice geometry remain unchanged. The original inventory remains 431 assets. Deterministic SVG wrappers embed complete originals and apply paint only to the embedded image. `build-unity-semantic.mjs` supplies 40 opt-in button, panel, product-card, popup/cap/footer, input and chart paints; its generated CSS declares variables only and never applies a page/global repaint. Secondary/outline controls and Dock tiles now use its utility paint with light ink and the original secondary-button geometry. Ghost buttons use the original flat-button image with its own slice/width triplet. Original navy panel/list/window frames are the information supports; availability does not require a bright fill. Neutral adapters remain where explicitly used for disabled/off states, inset controls and legacy chrome. Never filter a page/content ancestor, modify source PNGs or desaturate original colorful icons. These are custom application colors inspired by the reference, not exact brand RGB or native Apple colors.

## Quality bar

### Dock exception — selected study 2

The user rejected the first sprite-based approximation and explicitly requested the Dock be drawn in frontend code so it can grow with more buttons. Its visual target is `../../../.artifacts/dock-option2/reference.png`: continuous navy tray, dimensional blue rim, restrained light, cyan raised active seat with concave flared feet. This is a bounded exception to the source-only chrome rule, not permission to redraw Unity icons or repaint other pages. `console-dock-artwork.tsx` owns scalable SVG geometry; `game-dock.css` owns responsive dimensions and movement. No generated Dock bitmap ships. Preserve all links, permission gates, translated accessible names, menu behavior and the separate two-row settings Dock. Existing destination count determines width; no hard-coded eighth-slot background.

Source manifest, importer and paint-adapter regression tests; frontend typecheck, tests and production build; a bounded desktop/mobile inspection of changed surfaces and relevant dialogs. Labeled semantic paints must preserve at least 4.5:1 contrast across their tonal envelope and normal/hover/pressed brightness states; light materials require their matching dark ink, utility controls their light ink. These checks do not replace screenshot and interaction inspection. Earlier migration/gray/bright-panel captures are not acceptance evidence for this hierarchy correction. Current results and the bounded review verdict belong in `design-qa.md`; this brief does not certify every route or business flow. No Git push or deployment.

## Reference evidence

- User pack: `/Users/dax/Documents/UnityUITest/GUI Pro - Simple CasualPSD 1.0.7.unitypackage`.
- User-pinned Unity demo screenshots: `/var/folders/dk/5kr3ykh51tq9cy8l0n4fpyw80000gn/T/codex-clipboard-127d3887-1080-4463-b6df-ebe6e1983c38.png` and `/var/folders/dk/5kr3ykh51tq9cy8l0n4fpyw80000gn/T/codex-clipboard-f1331586-045d-42e5-8a56-157c0b34bb98.png`.
- Current bounded review captures: `../../../.impeccable/review/` and `../../../.artifacts/hierarchy-redesign/`; findings and limits are recorded in `../design-qa.md`.
- Original dark settings and component PSD composites in `/tmp/dotapi-unity-psd.eS63v7/`.
- Official demo: https://layerlab.itch.io/gui-pro-simple-casual-demo
- HIG: https://developer.apple.com/design/human-interface-guidelines/dark-mode
- Asset/provenance contract: `../../../docs/unity-ui-assets.md`.
